# دليل تشغيل متجر شنقيط (خطوة بخطوة)

مرحباً بك! لقد تم بناء مشروعك بالكامل وهو جاهز للعمل. بما أنك جديد في هذا المجال، اتبع هذه الخطوات البسيطة لتشغيل الموقع على جهازك.

## الخطوة 1: تحميل البرامج الضرورية
قبل البدء، يجب أن يكون لديك البرامج التالية مثبتة على جهازك:

1. **Node.js**:
   - حمل النسخة "LTS" من الرابط: [https://nodejs.org](https://nodejs.org)
   - ثبت البرنامج بالضغط على "Next" حتى النهاية.

2. **Docker Desktop**:
   - حمله من الرابط: [https://www.docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop)
   - ثبته وشغله. تأكد من أن أيقونة الحوت تظهر في شريط المهام بالأسفل.

3. **VS Code** (محرر الأكواد):
   - حمله من الرابط: [https://code.visualstudio.com](https://code.visualstudio.com)
   - ثبته على جهازك.

---

## الخطوة 2: فتح المشروع
1. افتح برنامج **VS Code**.
2. من القائمة العلوية، اضغط على **File** ثم **Open Folder**.
3. اذهب إلى المسار التالي وافتحه:
   `C:\Users\PC\.gemini\antigravity\scratch\shuniqit-store`

---

## الخطوة 3: تشغيل الأوامر
الآن سنقوم بتجهيز الموقع. داخل VS Code:

1. اضغط على **Terminal** في القائمة العلوية ثم **New Terminal**.
2. ستظهر لك شاشة سوداء في الأسفل. انسخ والصق الأوامر التالية واحداً تلو الآخر (اضغط Enter بعد كل أمر):

**أمر 1: تحميل الملفات الضرورية**
```bash
npm install
```
*(انتظر قليلاً حتى ينتهي التحميل)*

**أمر 2: تشغيل قاعدة البيانات**
```bash
docker-compose up -d
```
*(تأكد أن برنامج Docker يعمل قبل هذا الأمر)*

**أمر 3: تجهيز قاعدة البيانات**
```bash
npx prisma migrate dev
```
*(سيطلب منك اسماً، اكتب `init` واضغط Enter)*

**أمر 4: إضافة بيانات تجريبية (منتجات ومستخدمين)**
```bash
npx prisma db seed
```

**أمر 5: تشغيل الموقع!**
```bash
npm run dev
```

---

## الخطوة 4: تصفح الموقع
الآن الموقع يعمل!

1. افتح متصفح الإنترنت (Chrome أو Edge).
2. اكتب الرابط التالي: [http://localhost:3000](http://localhost:3000)
3. ستظهر لك الصفحة الرئيسية للمتجر.

### الدخول كمدير (Admin)
للوصول للوحة التحكم وإدارة المنتجات:
1. اذهب إلى: [http://localhost:3000/auth/login](http://localhost:3000/auth/login)
2. البريد الإلكتروني: `user1@example.com`
3. كلمة المرور: `password123`
4. بعد الدخول، اذهب إلى: [http://localhost:3000/admin](http://localhost:3000/admin)

---

## مبروك! 🎉
لديك الآن متجر إلكتروني متكامل يعمل على جهازك.
